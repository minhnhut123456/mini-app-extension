{
  "name": "zalo-mini-app-clone-monorepo",
  "private": true,
  "scripts": {
    "build:main-view": "pnpm -F main-view run build && copyfiles copyfiles -u 3 packages/main-view/dist/* packages/vscode-extension/main-view-dist",
    "dev:vscode-extension-esbuild": "pnpm -F zalo-mini-app-clone run watch:esbuild",
    "dev:vscode-extension-tsc": "pnpm -F zalo-mini-app-clone run watch:tsc",
    "vscode:publish": "pnpm run build:main-view && pnpm -F zalo-mini-app-clone build && node scripts/vscode-publish.js",
    "vscode:package": "pnpm run build:main-view && pnpm -F zalo-mini-app-clone build && node scripts/vscode-package.js"
  },
  "devDependencies": {
    "@vscode/vsce": "^3.7.1",
    "copyfiles": "^2.4.1"
  }
}
